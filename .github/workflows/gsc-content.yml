name: GSC Content Opportunities

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Build the issue but skip creating/updating it"
        required: false
        type: boolean
        default: false
  schedule:
    # Monthly on the 1st at 01:30 UTC (09:30 AM PHT). Content updates + new
    # supporting pages get cleaner signal on a monthly cadence than weekly
    # re-runs of the overlapping 28-day GSC window. New blog posts stay weekly
    # (see gsc-opportunities.yml).
    - cron: "30 1 1 * *"

permissions:
  contents: write
  issues: write

jobs:
  opportunities:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - run: npm ci

      - name: Find content opportunities
        env:
          GSC_SERVICE_ACCOUNT_JSON: ${{ secrets.GSC_SERVICE_ACCOUNT_JSON }}
          GSC_SITE_URL: ${{ secrets.GSC_SITE_URL }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # Optional — Langfuse layer is a clean no-op when unset.
          LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
          LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
          LANGFUSE_BASEURL: ${{ secrets.LANGFUSE_BASEURL }}
          # GitHub handle to @mention/assign when pages need updates.
          # Override with repo variable NOTIFY_GH_HANDLE; defaults to the repo owner.
          NOTIFY_GH_HANDLE: ${{ vars.NOTIFY_GH_HANDLE || github.repository_owner }}
          # How many content items (updates + new supporting pages) to auto-queue
          # per month (override via repo var CONTENT_PROMOTE_COUNT).
          PROMOTE_COUNT: ${{ vars.CONTENT_PROMOTE_COUNT || '4' }}
        run: |
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            node scripts/blog-agent/gsc-opportunities.mjs --track content --dry-run
          else
            node scripts/blog-agent/gsc-opportunities.mjs --track content
          fi

      - name: Open or update review issue
        if: ${{ inputs.dry_run != true }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NOTIFY_GH_HANDLE: ${{ vars.NOTIFY_GH_HANDLE || github.repository_owner }}
        run: |
          TITLE=$(cat /tmp/gsc-issue-content-title.txt)
          UPDATES=$(cat /tmp/gsc-issue-content-count.txt 2>/dev/null || echo 0)
          gh label create "gsc-content" --color "1D76DB" \
            --description "Monthly GSC content updates + new pages" 2>/dev/null || true
          # Rolling per-month issue: update if one with this exact title is open.
          EXISTING=$(gh issue list --state open --label "gsc-content" \
            --json number,title \
            --jq ".[] | select(.title==\"${TITLE}\") | .number" | head -1)
          if [ -n "$EXISTING" ]; then
            gh issue edit "$EXISTING" --body-file /tmp/gsc-issue-content.md
            NUM="$EXISTING"
            echo "Updated issue #$EXISTING"
          else
            URL=$(gh issue create --title "$TITLE" --body-file /tmp/gsc-issue-content.md \
              --label "gsc-content")
            NUM="${URL##*/}"
            echo "Created issue #$NUM"
          fi
          # Notify: assign the issue when ≥1 page needs an update. The @mention
          # in the body (added by the reporter) also triggers a notification.
          if [ "$UPDATES" -gt 0 ] && [ -n "$NOTIFY_GH_HANDLE" ]; then
            gh issue edit "$NUM" --add-assignee "$NOTIFY_GH_HANDLE" \
              && echo "Assigned #$NUM to $NOTIFY_GH_HANDLE ($UPDATES updates)" \
              || echo "Assign failed (handle not a collaborator?) — @mention still notifies."
          fi

      - name: Commit ledger + auto-queued topics
        if: ${{ inputs.dry_run != true }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Content ledger + any topics the analysis auto-promoted into the queue.
          if ! git diff --quiet scripts/blog-agent/gsc-suggestions-content-log.json scripts/blog-agent/topic-queue.json; then
            git add scripts/blog-agent/gsc-suggestions-content-log.json scripts/blog-agent/topic-queue.json
            git commit -m "gsc: content ledger + auto-queued updates (automated)"
            git pull --rebase origin main || true
            # Non-fatal: a protected main just means it isn't persisted in CI.
            git push || echo "Push skipped (protected branch?)"
          fi
